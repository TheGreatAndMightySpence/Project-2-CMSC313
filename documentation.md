This program prints the variable "prompt"

It will then take in the number and put it into the buffer "in"

Then we convert that buffer from ascii to integers then it will add it to a register.

In that register we add itself doubling the number.

Finally we use sprintf to convert the number back into ascii so we can print to back out to the command line
